# Optional extras for downloading & reading real Sentinel-1 GRD products
# from the Copernicus Data Space Ecosystem.
# Install with:  pip install -r requirements-sentinel.txt
# Without these the project uses the physics-based synthetic SAR generator.
pystac-client>=0.7
requests>=2.31
rasterio>=1.3
xarray>=2024.1
